package server.Result;

/**
 * Container class that returns success message for load service
 */
public class LoadResult extends Result {
    /**
     * Construct object with success message
     */
    public LoadResult(String message) {
        super(message);
    }
}
